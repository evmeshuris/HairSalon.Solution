@{
  Layout = "_Layout";
}

@model HairSalon.Models.Stylist

<h2>Are you sure you want to delete this stylist?</h2>

<h2>@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)</h2>
@using (Html.BeginForm())
{
    <input class="myCssClass" type="submit" value="Delete" />
  
}

@Html.ActionLink("Back to Stylists", "Index", null, null , new {@class="myCssClass"})